# File based VIM Bootstrap

These are just my config preferences to use in Nvim text editor

- insert into folder `nvim` (normally in `~/.config/nvim`)
- run `nvim`
- In Normal Mode run `:PlugInstall`

be happy
